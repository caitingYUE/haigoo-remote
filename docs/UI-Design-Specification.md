# Haigoo 网站 UI 设计规范文档

## 概述

本文档定义了 Haigoo 远程求职平台的完整 UI 设计规范，包括颜色系统、组件设计、交互规范等，确保整个应用的视觉一致性和用户体验。

## 1. 颜色系统

### 1.1 主品牌色
```css
--haigoo-primary: #803af2;           /* 主品牌紫色 */
--haigoo-primary-light: #9470ff;     /* 浅色变体 */
--haigoo-primary-dark: #6d28d9;      /* 深色变体 */
```

### 1.2 紫色色阶
```css
--haigoo-purple-50: #f9f7ff;
--haigoo-purple-100: #f3f0ff;
--haigoo-purple-200: #e9e2ff;
--haigoo-purple-300: #d6c9ff;
--haigoo-purple-400: #b8a3ff;
--haigoo-purple-500: #9470ff;
--haigoo-purple-600: #803af2;
--haigoo-purple-700: #7c3aed;
--haigoo-purple-800: #6d28d9;
--haigoo-purple-900: #5b21b6;
```

### 1.3 中性色系
```css
--gray-50: #f9fafb;
--gray-100: #f3f4f6;
--gray-200: #e5e7eb;
--gray-300: #d1d5db;
--gray-400: #9ca3af;
--gray-500: #6b7280;
--gray-600: #4b5563;
--gray-700: #374151;
--gray-800: #1f2937;
--gray-900: #111827;
--gray-soft: #6f6c76;
```

### 1.4 功能色彩
```css
--success: #10b981;     /* 成功绿色 */
--warning: #f59e0b;     /* 警告橙色 */
--error: #ef4444;       /* 错误红色 */
--info: #06b6d4;        /* 信息青色 */
```

## 2. 岗位卡片设计规范

### 2.1 设计理念
推荐页和全部岗位页采用两套不同的卡片设计方案，以区分内容层级和用户关注度：
- **推荐页卡片**: 突出推荐价值，视觉层次丰富，强调个性化匹配
- **全部岗位页卡片**: 信息密度高，便于快速浏览和比较，注重实用性

### 2.2 推荐页卡片设计

#### 2.2.1 TOP 3 推荐卡片（特殊样式）
```
┌─────────────────────────────────────────────────────┐
│                                            [TOP标签] │
│ [公司Logo] 职位标题                                   │
│           公司名称                                   │
│           📍 地点                        $薪资范围    │
│                                                     │
│ 职位描述 (最多2行)                                   │
│                                                     │
│ [技能1] [技能2] [技能3]                              │
│                                                     │
│ [立即申请 - 渐变按钮]              [收藏 - 渐变按钮] │
└─────────────────────────────────────────────────────┘
```

**视觉特征**:
- **容器**: 渐变背景 + 装饰性背景元素
- **尺寸**: 更大的内边距 `p-8`，更高的卡片高度
- **TOP标签**: 右上角倾斜标签，渐变背景，阴影效果
- **公司Logo**: 更大尺寸 `w-14 h-14`，带光环效果
- **薪资**: 渐变文字效果，更突出的字体大小 `text-xl`
- **按钮**: 渐变背景，悬停缩放效果
- **装饰元素**: 半透明圆形背景装饰

**样式规范**:
```css
/* TOP 1 卡片 */
.top-1-card {
  background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
  box-shadow: 0 20px 40px rgba(251, 191, 36, 0.3);
}

/* TOP 2 卡片 */
.top-2-card {
  background: linear-gradient(135deg, #a855f7 0%, #9333ea 100%);
  box-shadow: 0 20px 40px rgba(168, 85, 247, 0.3);
}

/* TOP 3 卡片 */
.top-3-card {
  background: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%);
  box-shadow: 0 20px 40px rgba(6, 182, 212, 0.3);
}
```

#### 2.2.2 普通推荐卡片（4-6位）
```
┌─────────────────────────────────────────────────────┐
│ [公司Logo] 职位标题                                   │
│           公司名称                                   │
│           📍 地点                        $薪资范围    │
│                                                     │
│ 职位描述 (最多2行)                                   │
│                                                     │
│ [技能1] [技能2] [技能3] +N更多                       │
│                                                     │
│ [立即申请]                              [收藏]       │
└─────────────────────────────────────────────────────┘
```

**视觉特征**:
- **容器**: 白色背景，简洁边框
- **尺寸**: 标准内边距 `p-6`
- **公司Logo**: 标准尺寸 `w-12 h-12`
- **薪资**: 品牌色显示 `text-violet-600`
- **按钮**: 标准样式，无渐变效果

### 2.3 全部岗位页卡片设计

#### 2.3.1 标准列表卡片
```
┌─────────────────────────────────────────────────────┐
│ [公司Logo] 职位标题                        [外链按钮] │
│           公司名称                                   │
│           薪资 | 地点 | 类型 | 发布时间              │
│           [标签1] [标签2] [标签3] [标签4]            │
├─────────────────────────────────────────────────────┤
│ 职位描述 (最多2行)                                   │
├─────────────────────────────────────────────────────┤
│ [技能1] [技能2] [技能3] [技能4] [技能5] +N更多       │
├─────────────────────────────────────────────────────┤
│ 在 XXX 查看                              点击查看详情 │
└─────────────────────────────────────────────────────┘
```

**视觉特征**:
- **容器**: 白色背景，细边框，简洁设计
- **信息密度**: 高密度信息展示，便于快速扫描
- **分割线**: 使用细分割线区分内容区域
- **外链按钮**: 右上角半透明按钮，悬停显示
- **底部操作**: 简化的操作区域，突出实用性

### 2.4 卡片设计对比总结

| 特征 | 推荐页TOP卡片 | 推荐页普通卡片 | 全部岗位页卡片 |
|------|---------------|----------------|----------------|
| **背景** | 渐变背景 + 装饰元素 | 白色背景 | 白色背景 |
| **内边距** | `p-8` (32px) | `p-6` (24px) | `p-6` (24px) |
| **Logo尺寸** | `w-14 h-14` (56px) | `w-12 h-12` (48px) | `w-12 h-12` (48px) |
| **薪资样式** | 渐变文字 `text-xl` | 品牌色 `text-lg` | 品牌色 `text-sm` |
| **按钮样式** | 渐变背景 + 缩放 | 标准样式 | 无按钮 |
| **信息密度** | 低密度，突出展示 | 中密度 | 高密度 |
| **特殊元素** | TOP标签 + 装饰 | 无 | 外链按钮 |
| **分割线** | 无 | 无 | 有 |
| **交互重点** | 视觉吸引 | 平衡展示 | 快速浏览 |

### 2.5 响应式适配

#### 2.5.1 推荐页卡片响应式
- **桌面端**: `lg:grid-cols-3` - TOP卡片保持3列布局
- **平板端**: `md:grid-cols-2` - TOP卡片调整为2列
- **移动端**: `grid-cols-1` - 所有卡片单列显示，TOP标签位置调整

#### 2.5.2 全部岗位页卡片响应式
- **所有设备**: 始终保持单列布局 `grid-cols-1`
- **移动端优化**: 隐藏部分次要信息，保持核心信息可见

### 2.6 公司Logo设计
- **推荐页TOP卡片**: `w-14 h-14` (56px × 56px)
- **其他卡片**: `w-12 h-12` (48px × 48px)
- **圆角**: `rounded-xl` (12px)
- **颜色**: 基于公司名称生成，6种预设颜色循环
- **文字**: 公司名首字母，白色，`font-semibold text-sm`

### 2.7 文字层级

#### 2.7.1 推荐页TOP卡片文字层级
- **职位标题**: `font-bold text-lg text-gray-800 dark:text-white`
- **公司名称**: `font-medium text-gray-600 dark:text-gray-300`
- **薪资**: `font-bold text-xl` + 渐变色效果
- **描述文字**: `text-gray-700 dark:text-gray-300 text-sm`
- **技能标签**: `text-sm font-medium`

#### 2.7.2 推荐页普通卡片文字层级
- **职位标题**: `font-bold text-gray-900 dark:text-white text-lg`
- **公司名称**: `font-medium text-gray-600 dark:text-gray-400`
- **薪资**: `font-bold text-xl text-violet-600 dark:text-violet-400`
- **描述文字**: `text-gray-700 dark:text-gray-300 text-sm`
- **技能标签**: `text-sm font-medium`

#### 2.7.3 全部岗位页卡片文字层级
- **职位标题**: `font-semibold text-gray-900 text-lg`
- **公司名称**: `font-medium text-gray-600 text-sm`
- **核心信息**: `text-sm`
- **描述文字**: `text-gray-600 text-sm`
- **标签文字**: `text-xs font-medium`

### 2.8 薪资显示规则

#### 2.8.1 通用格式化规则
- **显示条件**: 仅当 `salary.min > 0` 时显示
- **格式化**: 
  - 万元以上: `X.X万` 格式
  - 万元以下: 千分位分隔
  - 相同薪资: 显示单一数值
  - 不同薪资: `最低 - 最高` 格式

#### 2.8.2 不同卡片类型的薪资样式
- **推荐页TOP卡片**: 渐变文字效果 + `text-xl` + `font-bold`
- **推荐页普通卡片**: `text-violet-600 dark:text-violet-400` + `text-xl` + `font-bold`
- **全部岗位页卡片**: `text-haigoo-primary font-semibold` + `text-sm`

### 2.9 工作类型标签

#### 2.9.1 标签颜色系统
```css
.job-type-full-time { bg-green-100 text-green-700 }
.job-type-part-time { bg-blue-100 text-blue-700 }
.job-type-contract { bg-orange-100 text-orange-700 }
.job-type-freelance { bg-purple-100 text-purple-700 }
.job-type-internship { bg-pink-100 text-pink-700 }
```

#### 2.9.2 不同卡片类型的标签样式
- **推荐页卡片**: `px-3 py-1 rounded-full text-sm font-medium`
- **全部岗位页卡片**: `px-2 py-1 rounded text-xs font-medium`

### 2.10 技能标签

#### 2.10.1 标签颜色和样式
- **主要技能**: `bg-haigoo-primary/10 text-haigoo-primary`
- **更多提示**: `bg-gray-100 text-gray-500`

#### 2.10.2 不同卡片类型的技能标签样式
- **推荐页卡片**: `px-3 py-1 rounded-full text-sm font-medium`
- **全部岗位页卡片**: `px-2 py-1 rounded text-xs font-medium`

#### 2.10.3 显示规则
- **推荐页**: 最多显示3个技能 + "更多"提示
- **全部岗位页**: 最多显示4个技能，超出部分省略

## 3. 交互设计规范

### 3.1 悬停效果

#### 3.1.1 推荐页TOP卡片悬停效果
```css
.top-job-card:hover {
  transform: translateY(-8px) scale(1.02);
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
```

#### 3.1.2 推荐页普通卡片悬停效果
```css
.recommended-job-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
}
```

#### 3.1.3 全部岗位页卡片悬停效果
```css
.job-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  border-color: #8b5cf6;
  transition: all 0.2s ease;
}
```

### 3.2 焦点管理

#### 3.2.1 键盘导航支持
- **Tab键导航**: 所有卡片支持Tab键导航
- **Enter键激活**: 按Enter键打开职位详情
- **焦点样式**: `focus:ring-2 focus:ring-haigoo-primary focus:ring-offset-2`

#### 3.2.2 不同卡片类型的焦点处理
- **推荐页TOP卡片**: 更明显的焦点环，`ring-4`
- **推荐页普通卡片**: 标准焦点环，`ring-2`
- **全部岗位页卡片**: 简化焦点环，`ring-1`

### 3.3 无障碍设计

#### 3.3.1 ARIA标签规范
- **卡片角色**: `role="button"` 或 `role="article"`
- **标签描述**: `aria-label` 包含完整职位信息
- **状态指示**: `aria-pressed` 用于收藏状态

#### 3.3.2 不同卡片类型的无障碍处理
- **推荐页TOP卡片**: 
  - `aria-label="推荐职位: {title} - {company} - {salary}"`
  - `aria-describedby` 指向详细描述
- **推荐页普通卡片**: 
  - `aria-label="职位: {title} - {company}"`
- **全部岗位页卡片**: 
  - `aria-label="职位列表项: {title} - {company} - {location}"`

- **屏幕阅读器**: 优化的文本描述
- **最小点击区域**: 44px × 44px

## 4. 响应式设计

### 4.1 网格布局

#### 4.1.1 推荐页布局
- **TOP卡片区域**:
  - 桌面端: `lg:grid-cols-3` (3列)
  - 平板端: `md:grid-cols-2` (2列)  
  - 移动端: `grid-cols-1` (1列)
- **普通推荐卡片**:
  - 桌面端: `lg:grid-cols-2` (2列)
  - 平板端: `md:grid-cols-1` (1列)
  - 移动端: `grid-cols-1` (1列)

#### 4.1.2 全部岗位页布局
- **所有设备**: 始终保持 `grid-cols-1` 单列布局
- **卡片宽度**: `max-w-4xl mx-auto` 居中显示

### 4.2 间距调整

#### 4.2.1 推荐页间距
- **TOP卡片间距**: `gap-8` (桌面) / `gap-6` (移动)
- **普通卡片间距**: `gap-6` (桌面) / `gap-4` (移动)
- **区域间距**: `mb-12` (桌面) / `mb-8` (移动)

#### 4.2.2 全部岗位页间距
- **卡片间距**: `gap-4` (所有设备)
- **内边距**: `p-6` (桌面) / `p-4` (移动)
- **内容间距**: `space-y-4` / `space-y-3`

### 4.3 字体大小调整

#### 4.3.1 推荐页字体响应式
- **TOP卡片职位标题**: `text-xl` (桌面) / `text-lg` (移动)
- **TOP卡片薪资**: `text-2xl` (桌面) / `text-xl` (移动)
- **普通卡片职位标题**: `text-lg` (桌面) / `text-base` (移动)
- **普通卡片薪资**: `text-xl` (桌面) / `text-lg` (移动)

#### 4.3.2 全部岗位页字体响应式
- **职位标题**: `text-lg` (桌面) / `text-base` (移动)
- **薪资**: `text-sm` (所有设备)
- **描述文字**: `text-sm` (桌面) / `text-xs` (移动)

## 5. 动画与过渡

### 5.1 标准过渡
```css
transition-all duration-300 cubic-bezier(0.4, 0, 0.2, 1)
```

### 5.2 悬停动画
- **缩放效果**: `hover:scale-102`
- **阴影过渡**: `hover:shadow-lg`
- **颜色过渡**: `hover:text-haigoo-primary`

## 6. 组件状态管理

### 6.1 加载状态
- **骨架屏**: 使用 `animate-pulse` 效果
- **加载指示器**: 旋转动画 `animate-spin`

### 6.2 错误状态
- **错误图标**: Lucide React 图标
- **错误文字**: `text-red-600 dark:text-red-400`

### 6.3 空状态
- **空状态图标**: 大尺寸灰色图标
- **提示文字**: 友好的引导文案

## 7. 暗色模式支持

### 7.1 背景色
- **主背景**: `bg-gray-50 dark:bg-gray-900`
- **卡片背景**: `bg-white dark:bg-gray-800`

### 7.2 文字色
- **主文字**: `text-gray-900 dark:text-white`
- **次要文字**: `text-gray-600 dark:text-gray-300`

## 8. 开发规范

### 8.1 CSS类命名
- 使用 Tailwind CSS 原子类
- 自定义类使用 BEM 命名规范
- 组件样式封装在对应的 `.tsx` 文件中

### 8.2 组件结构
- 使用语义化 HTML 标签
- 合理的组件拆分和复用
- Props 接口明确定义

### 8.3 性能优化
- 图片懒加载
- 组件按需渲染
- 避免不必要的重渲染

## 9. 质量检查清单

### 9.1 视觉检查
- [ ] 颜色使用符合规范
- [ ] 字体大小层级正确
- [ ] 间距统一协调
- [ ] 响应式布局正常

### 9.2 交互检查
- [ ] 悬停效果正常
- [ ] 键盘导航可用
- [ ] 焦点管理正确
- [ ] 无障碍标签完整

### 9.3 功能检查
- [ ] 数据显示正确
- [ ] 链接跳转正常
- [ ] 错误处理优雅
- [ ] 加载状态清晰

---

**版本**: v1.0  
**更新日期**: 2025年11月  
**维护者**: Haigoo 开发团队