> 执行状态（2026-09-13）：后端修复已上线并通过接口检查；实际部署与故障处理以 [上线记录](../../releases/1.0.38-production-candidate.md) 为准。以下为原始准备计划，不能作为当前发布状态。

# Mini Program 1.0.38 Release Preparation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** 固定用户已通过真机验收的 1.0.38，完成后端上线候选、部署预检、回滚准备及微信提审交接。

**Architecture:** 沿用 Vercel 网站/API、生产 CloudRun 和独立微信上传流程。后端按 Vercel → CloudRun 顺序发布；前端使用已上传且已验收的 1.0.38。

**Tech Stack:** Taro/React/TypeScript、Node.js、PostgreSQL、Vercel、CloudBase CloudRun、微信开发者工具 CLI。

**Spec:** `docs/qa/2026-09-13-account-directory.md`、`docs/releases/1.0.38-device-acceptance.md`，以及用户本轮确认“真实验收已经通过”。

## Global Constraints

- 版本保持 1.0.38；不变更用户已经验收的前端包。
- production CloudBase `cloud1-d8ggt7rbl273f83c7`，CloudRun `haigoo-mini-prod`，AppID `wxc61db5b8b472925a`。
- 虚拟支付环境保持 0；不修改支付密钥、会员记录、账号记录，不执行数据库迁移或数据修复。
- 上线准备与实际切换生产流量、提交微信审核、审核通过后发布分开记录。
- 仅使用隔离数据库验证账号写入；线上探测不触发真实注册、邮件、解绑或注销。

## Task 1: 固定候选及回滚基线

**Files:** `docs/releases/1.0.38-production-candidate.md`、发布清单 JSON、现有 QA 记录。

- [x] 核对生产 Vercel 实际部署：`dpl_DaSFjE3tjuJKNSPjwwSLf8RMtWwx`，地址 `haigoo-remote-aac6mcvg9-caitlinyct.vercel.app`，来源提交 `9a06d5e85919f7fa88034a6c7db4a93e45eae538`。
- [x] 核对 CloudRun `/health` 的源码摘要等于本地 HEAD 四个运行文件摘要 `dc4166e3b61f8a334ea7e5f72b1b0963edc787e52555271a121574f8d139ba62`。
- [x] 确认当前 HEAD 相对 Vercel 来源提交没有网站/API 运行源码增量；本轮网站运行变更仅 `api/auth.js`、`lib/api-handlers/mini-gateway.js`。
- [x] 将本轮代码、测试、验收记录提交为本地可追溯候选，保存发布清单、候选与旧 CloudRun 源码归档。

## Task 2: 部署预检与构建

**Files:** 现有 `test-mini-account-lifecycle.js`、`test-mini-account-client-races.js`、`scripts/check-admin-release-contracts.mjs`、`miniprogram/scripts/check-production-assets.mjs`、`.vercel/output`。

- [x] 执行 `npm run build`，网站 TypeScript 和生产构建通过。
- [x] 执行 `npm --prefix miniprogram run type-check` 和 `npm --prefix miniprogram run check:weapp:prod`，核对原已上传包 SHA-256。
- [x] 执行账号生命周期、账号请求竞态、网关签名、发布合同和管理员发布合同测试。
- [x] 使用 `vercel deploy --dry --json` 审计上传文件；排除密钥、本地测试材料和微信包。
- [x] 获取当前 production 构建配置并执行 `vercel build --prod`，生成候选 serverless 构建产物；记录构建成功和源码摘要。

## Task 3: 发布交接

**Files:** `docs/releases/1.0.38-production-candidate.md`。

- [x] 将用户真机验收状态更新为“用户确认通过”，保留此前自动化验证的测试边界。
- [x] 记录生产部署命令、部署后 `/health` 摘要和只读接口探测命令、回滚目标和微信审核版本说明。
- [x] 准备完成时明确报告生产流量及微信审核的实际状态。

## 正式执行顺序（准备阶段不勾选为已部署）

1. 从固定候选执行 Vercel production 部署；核验部署来源提交和域名指向。Vercel 完成后再执行 `node scripts/deploy-mini-cloudrun.mjs --target=production`。
2. CloudRun 必须连续返回候选源码摘要 `6e26516a030b8b7ec63f2b38fe015abc73683a100adaa86f97bd3eaba623824b`，并保持 MINIAPP/OA、1–2 实例、支付环境 0。
3. 执行 `node scripts/verify-mini-gateway.mjs --target=production --via-cloudrun --action=career_watch_options`、同方式 `companies` 与 `membership_plans`；检查非授权账号写入口仍拒绝无效请求。
4. 微信公众平台提交已验收的 1.0.38 审核；审核通过后再执行微信线上发布。后台平台访问限制不能视作已完成操作。
5. 若后端验证失败，不提审。按候选记录回滚 Vercel 和 CloudRun 至本次核对的上线前版本，不回滚数据库业务数据。
