# 小程序 1.0.38 真机验收包

上传日期：2026-09-13（Asia/Shanghai），基于 1.0.37 的企业列表和账号交互修复。

## 发布状态

- 微信开发版本上传成功：CLI `✔ upload`，版本 `1.0.38`，包大小 **1,113,840 bytes**。
- AppID：`wxc61db5b8b472925a`。
- 渠道：production；沿用现有接口，不是发布正式版。
- CloudBase：`cloud1-d8ggt7rbl273f83c7`；CloudRun：`haigoo-mini-prod`；接口来源：`https://haigooremote.com`。
- 上传目录：`miniprogram/.wechat-production/dist`。
- 构建文件集合 SHA-256：`ee5ec34cb014aff6914cd31754a78c09f99771e3bb1f9c97b43102404c2a5047`（按相对路径排序，依次纳入路径、NUL 分隔与文件内容）。
- 上传前 production 资源门禁和现有生产 CloudRun 的 options/companies 只读检查通过。
- 未提交审核或发布正式版。公众平台访问受浏览器策略限制，不能确认“设为体验版”状态；需在版本管理确认体验版指向 1.0.38。

## 包内修复

企业页定期自动同步、后台更新保留列表、移除常驻检查入口、临时窄条更新提示；账号表单重复提交保护、退出后旧请求保护、错误密码不误退出、成功后页面栈及本机账号快照清理、注册成功邮件反馈。

保留 1.0.37 的 Match 手动滑动与页面缓存稳定性修复。上传包没有模拟器测试数据或接口替身。

## 后端状态

**以下修复在本地并已通过隔离数据库测试，未随本次前端上传部署：**

- `api/auth.js`：重复注册不删除已存在账号，取消测试邮箱登录强制升权。
- `lib/api-handlers/mini-gateway.js`：绑定冲突原子保护、密码错误码、停用账号拒绝、解绑/注销当前账号校验。
- `cloudrun/index.mjs`：解绑/注销携带签名会话中的 userId。

后台尚未部署，因此不能把 1.0.38 前端验收通过等同于生产账号链路所有修复已生效。

## 验证与真机重点

详细结果及微信模拟器截图见 [QA 记录](../qa/2026-09-13-account-directory.md)。19 项相关回归、账号生命周期 PostgreSQL 隔离测试、TypeScript、语法与生产构建门禁通过。

真机重点：企业页往返和行业切换不空屏；自动更新时仅出现窄条且自动收回；后台停留再返回保持合理列表；密码错误仍可重试；注销两次取消都不发请求。真实注销、解绑和邮箱回跳必须使用专用验收账号，不能使用真实业务账号做破坏性测试。
