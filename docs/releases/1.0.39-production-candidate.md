# 小程序 1.0.39 正式候选部署

用户于 2026-09-14 在本任务确认真机验收完成，授权准备正式部署。**正式后端已切换，微信正式候选包 1.0.39 已上传成功，审核由用户自行提交。**

## 发布范围

保留企业列表自动静默更新、页面状态稳定和账号交互优化；补齐岗位上新事件、微信一次性授权、持久通知队列、有限重试、幂等与授权失效状态同步。包含已上线的管理员岗位写入修复 `ceaced73`，不回退其他已提交功能。

本次正式候选源码为 `749815ef`（运行时代码最新提交 `ceaced73`，提醒修复 `ea33be8e`）。小程序仍为 **1.0.39**，正式包使用 `cloud1-d8ggt7rbl273f83c7` / `haigoo-mini-prod`，后端域名 `https://haigooremote.com`。

## 已完成准备

- 正式迁移 089 已通过同一 Neon 事务执行，共 29 条语句；关注记录迁移前后均为 42，迁移后未准备事件 0、待发送通知 0。
- 正式微信模板“新职位更新提醒”已核对，跳转状态配置为 `formal`，提醒开关已配置开启；保留原有 `CRON_SECRET`。
- 正式 CloudRun `/health` 返回 200；运行源码与本地一致：`6e26516a030b8b7ec63f2b38fe015abc73683a100adaa86f97bd3eaba623824b`。复用现有代理，最少 1、最多 2 实例，入口 OA/MINIAPP，支付环境为 0。
- 17 组提醒隔离回归、提醒客户端回归、管理员岗位写入、发布合同、Cron 策略及小程序类型检查通过；正式构建与资源检查通过。构建保留既有 CSS 导入顺序警告。
- 正式小程序归档：`artifacts/releases/1.0.39/wechat-production.tar.gz`，SHA-256：`b05839827deebcede30390b14ea491fb9452eb6d9c04e20dd526fe25236fef3f`。

## 部署状态

Vercel 部署 `dpl_6pceeacB2j1PK73z4MnC565JxDZF` / `https://haigoo-remote-mtejx0rks-caitlinyct.vercel.app` 已完成云端 Linux 构建并 promote，状态 `Ready`；再次 inspect `https://haigooremote.com` 确認指向该部署。使用候选验证后切换流程，依据 [Vercel CLI 部署说明](https://vercel.com/docs/cli/deploy)。

- 独立候选的提醒选项、企业列表、会员方案、首页、目录同步 5 项接口均返回 200 并通过业务合同检查。
- 未携带授权和仅伪造 `x-vercel-cron` 的请求均返回 401；企业图片返回 200，解码为 32×32 WebP。
- 切换后经正式 CloudRun 两轮检查，8/8 通过；含 SDK 和网络在内的采样耗时 1.364–5.504 秒，不等于用户页面加载耗时。
- 支付配置检查通过：正式支付环境 0、体验环境 1，两边未签名支付回调均返回 401；未执行交易。
- 微信 CLI 确认 AppID `wxc61db5b8b472925a`，返回 `✔ upload` 和 `Uploaded WeChat production bundle 1.0.39`。上传包 1,115,924 字节；备注 **Haigoo Remote production 1.0.39**。
- 微信后台 UI 核对因电脑操作工具的 URL 访问限制而停止，未声称已在页面确认；上传成功以 CLI 返回结果为证。本轮没有点击提交审核或发布。
- 2026-09-14 10:55:06（北京时间）正式提醒调度返回 200，日志确认消费者正常执行：prepared/sent/retry/failed/unknown/pending/unresolved 均为 0。该次为平台定时调度，不是人工通知测试。后续每 5 分钟处理符合有效授权条件的新增机会。

结构化证据见 [1.0.39 发布清单](1.0.39-release-manifest.json)。

## 回退

本轮切换前实际生产基线：`dpl_GuPHzeSUTyAwfag2G8kiH3X2z8vq` / `https://haigoo-remote-pxyoxbjek-caitlinyct.vercel.app`。必要时先确认没有后续生产增量，再回退该部署。该基线未启用新的提醒消费者。

迁移为新增结构与授权元数据维护；回退应用保留新增表、列、触发器及用户关注数据。不要删除通知记录或将 `unknown` 改回 `pending`，不要同时运行旧爬虫发送入口。仅修改项目环境变量不会改变已有部署，关闭提醒开关后需重新部署使其生效。

## 微信审核说明

> 优化企业列表更新及页面切换体验，完善账号登录与绑定流程；修复岗位上新订阅提醒，改善提醒授权、取消及状态同步。

选择版本时须核对备注 **Haigoo Remote production 1.0.39**，避免选中同版本号的旧体验包。本任务不提交微信审核，也不发布微信线上版本。

真实设备验收由用户确认；本轮部署验证不创建真实订单、不付款退款、不注销账号、不人工触发向真实用户发送通知。接口与隔离回归不能替代长期运行可用性保证。
