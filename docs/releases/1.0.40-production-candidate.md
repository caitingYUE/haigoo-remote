# 小程序 1.0.40 正式候选发布记录

2026-09-14：用户确认体验版真机验收通过后，本次已完成正式数据库迁移、Vercel 后端切换和微信 production 包上传。**微信审核由用户提交，本任务没有提交审核或发布微信线上版本。**

## 发布内容

源码为 `2a71c83bf70afd05de6c696265187c5ea998c5f8`，与已验收体验版一致。包含企业关注与一次提醒入口分离、岗位偏好匹配限制、匹配岗位更新页面及未读角标调整。

## 已核对结果

- 090 迁移成功，通过既有迁移工具事务执行 2 条 SQL；关注记录迁移前后均为 47。匹配函数存在；未删除用户记录。
- 22 组提醒隔离回归通过，包括偏好匹配、失效授权、重复执行与领取前再校验。
- 正式 Vercel 候选从精确 Git 提交归档构建，使用 production 配置；候选 5 项接口全部通过，缺少 Cron 授权和伪造 Cron 标头均返回 401。
- promote 成功，正式域名 inspect 确认指向 `dpl_HzTDVELfLoZrYBekbHsRpT2ZXhpc` / `https://haigoo-remote-eqx7oci5e-caitlinyct.vercel.app`，状态 Ready。
- 正式 CloudRun `cloud1-d8ggt7rbl273f83c7` / `haigoo-mini-prod` 健康检查 200，源码指纹 `6e26516a030b8b7ec63f2b38fe015abc73683a100adaa86f97bd3eaba623824b` 与本地一致，复用现有代理。最少 1、最多 2 实例，支付环境 0。
- 切换后经 CloudRun 到 Vercel 的三轮只读检查 12/12 通过；请求耗时 1415–5894 ms（含 SDK/网络）。有限探测不等同于长期可用性保证或压测。
- 本地正式包环境、服务、版本、资源门禁通过。微信 CLI 明确返回 `✔ upload` 与 `Uploaded WeChat production bundle 1.0.40`；AppID 为 `wxc61db5b8b472925a`，上传大小 1,120,259 字节。
- 本轮不创建订单、不修改用户资料、不人工发送真实提醒。已在体验环境完成的真机验收由用户确认；本次未独立查看微信后台版本页面，上传依据为 CLI 明确回执。

## 微信后台选择

版本：**1.0.40**；备注：**Haigoo Remote production 1.0.40**。请选择这份正式候选，避免误选同版本号的 experience 包。

审核说明建议：优化企业关注与岗位更新提醒交互，完善匹配岗位更新展示和未读状态，提升页面使用体验。

## 归档与回退

结构化证据：[发布清单](1.0.40-release-manifest.json)。本地上传回执：`tmp/mini-1040-production-upload.log`。

正式包归档：`artifacts/releases/1.0.40/wechat-production.tar.gz`，SHA-256 `ec59c6c3784555da97cc00d26f9b3010f2cd7bde5659dc11ecf7eb750e8a60bf`。

切换前部署：`dpl_6pceeacB2j1PK73z4MnC565JxDZF` / `https://haigoo-remote-mtejx0rks-caitlinyct.vercel.app`。如需回退，先确认没有后续正式增量，再 promote 该部署。兼容迁移的函数及视图保留，勿删除通知记录、用户关注或重置已消费授权。
