# 小程序 1.0.40 正式候选发布记录

2026-09-14：用户确认体验版真机验收通过后，本次已完成正式数据库迁移、Vercel 后端切换和微信 production 包上传。15:24 完成岗位更新详情修复的后端切换与同版本 production 包覆盖上传；15:53 覆盖上传订阅岗位总数与已读红点交互修复；随后又覆盖上传头像红点尺寸与贴边位置微调。**微信审核由用户提交，本任务没有提交审核或发布微信线上版本。**

## 发布内容

当前源码为 `ab0713ef`。在原 1.0.40 候选基础上，修复匹配岗位更新误判失效及详情路由缺少 `companyId` / `jobId` 的问题；“未读岗位更新”改为“订阅岗位更新”，入口数字展示全部岗位数，未读状态改用红点，并在进入页面后批量标记已读、同步清除头像红点。头像红点调整为更接近微信的贴边尺寸与位置。

## 已核对结果

- 090 迁移成功，通过既有迁移工具事务执行 2 条 SQL；关注记录迁移前后均为 47。匹配函数存在；未删除用户记录。
- 22 组提醒隔离回归通过，包括偏好匹配、失效授权、重复执行与领取前再校验。
- 正式 Vercel 候选从精确 Git 提交归档构建，使用 production 配置；候选 5 项接口全部通过，缺少 Cron 授权和伪造 Cron 标头均返回 401。
- promote 成功，正式域名 inspect 确认指向 `dpl_GXFL1sYe42UPQBkKjwjUsPHX2rEz` / `https://haigoo-remote-l25c7t2fc-caitlinyct.vercel.app`，状态 Ready。
- 正式 CloudRun `cloud1-d8ggt7rbl273f83c7` / `haigoo-mini-prod` 健康检查 200，源码指纹 `6e26516a030b8b7ec63f2b38fe015abc73683a100adaa86f97bd3eaba623824b` 与本地一致，复用现有代理。最少 1、最多 2 实例，支付环境 0。
- 修复候选 5 项接口与两项鉴权边界检查通过；切换后经 CloudRun 到 Vercel 的三轮只读检查 12/12 通过，请求耗时 1612–5909 ms（含 SDK/网络）。有限探测不等同于长期可用性保证或压测。
- 本地正式包环境、服务、版本、资源门禁通过。微信 CLI 明确返回 `✔ upload` 与 `Uploaded WeChat production bundle 1.0.40`；AppID 为 `wxc61db5b8b472925a`，本次上传大小 1,120,270 字节。
- 订阅岗位交互修复通过 TypeScript、定向 ESLint、Career Watch、Match 关注链路、运行时兼容、设计规范和发布门禁检查；production 包再次明确返回 `✔ upload`，最新上传大小 1,122,240 字节。该改动复用既有已读接口，不需要新的后端部署。
- 头像红点微调后再次通过 TypeScript、定向回归、ESLint、production 构建和资源门禁；微信 CLI 返回 `✔ upload`，最终上传大小 1,122,248 字节。
- 本轮不创建订单、不修改用户资料、不人工发送真实提醒。已在体验环境完成的真机验收由用户确认；本次未独立查看微信后台版本页面，上传依据为 CLI 明确回执。

## 微信后台选择

版本：**1.0.40**；备注：**Haigoo Remote production 1.0.40**。请选择这份正式候选，避免误选同版本号的 experience 包。

审核说明建议：优化企业关注与岗位更新提醒交互，完善匹配岗位更新展示和未读状态，提升页面使用体验。

## 归档与回退

结构化证据：[发布清单](1.0.40-release-manifest.json)。本地上传回执：`tmp/mini-1040-production-upload.log`。

修复包归档：`artifacts/releases/1.0.40/wechat-production-hotfix-f2bd7d53.tar.gz`，SHA-256 `7fa03ed3f45b06e5ff0c1a18f524d64de53bf3ada55655aaf001f424e807e94f`。

最新订阅岗位交互包归档：`artifacts/releases/1.0.40/wechat-production-read-state-76b5f494.tar.gz`，SHA-256 `901c426814d7abe690db68e150250332432bf198fc04b6dcbbad1f5155b6a5e6`。

最终头像红点微调包归档：`artifacts/releases/1.0.40/wechat-production-avatar-rim-ab0713ef.tar.gz`，SHA-256 `ab7030952121e7c4df995208e3666dc28835b8fb5f65adf8121d0609baa5cefd`。

本次切换前部署：`dpl_HzTDVELfLoZrYBekbHsRpT2ZXhpc` / `https://haigoo-remote-eqx7oci5e-caitlinyct.vercel.app`。如需回退，先确认没有后续正式增量，再 promote 该部署。兼容迁移的函数及视图保留，勿删除通知记录、用户关注或重置已消费授权。
