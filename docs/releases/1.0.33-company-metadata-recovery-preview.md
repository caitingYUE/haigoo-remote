# 1.0.33 企业元数据恢复预览

状态：待生产数据回填审批  
生成日期：2026-09-12  
生产写入：尚未执行

## 事故结论

- `stream-crawl-trusted-jobs` 在 2026-09-10 18:15 至 2026-09-11 12:16（Asia/Shanghai）运行四批，共改写 41 家人工录入企业。
- 爬虫读取对象遗漏企业元数据，随后调用全量 UPSERT，导致行业被写为“其他”，并清空员工数、成立年份、评分、标签、专长、翻译及部分招聘邮箱。
- 生产 Cron 已在部署 `dpl_4695KV9Y718mK56momc5CG6WyNqG` 中移除；旧入口返回 HTTP 410。
- Neon 当前历史保留窗口为 6 小时，事故前时间点已经过期；项目没有可用快照。
- Neon `develop` 分支保存了 2026-09-09 的事故前企业副本，可恢复 26 家；其余 15 家没有现存副本。

## 可恢复范围

| 字段 | 可恢复企业数 | 回填规则 |
|---|---:|---|
| 行业 | 26 | 仅当生产值为空或“其他”，且副本值为具体分类 |
| 员工数 | 26 | 仅填生产空值 |
| 成立年份 | 23 | 仅填生产空值 |
| 企业评分 | 16 | 仅填生产空值；不把 `0` 当作有效评分 |
| 评分来源 | 16 | 仅随有效评分恢复 |
| 标签 | 8 | 仅当生产数组为空 |
| 专长 | 19 | 仅当生产数组为空 |
| 翻译 | 24 | 仅当生产对象为空 |
| 招聘邮箱 | 15 | 仅填生产空值，不覆盖后续人工修改 |

地址不在本次自动回填范围内。该爬虫路径保留了原地址，41 家中只有 Mx51 当前缺少地址，且事故前副本不存在，不能可靠推断。

不恢复 `cover_image`。事故前副本中存在体积很大的 base64 图片，重新写回会增加数据库负担并再次造成媒体加载问题。

## 可从副本恢复的 26 家

AG1、Adapty.io、Agora、Candidate.fyi、Fuelerate、Gain、Kit、Lionbridge、Lovable、New Energy Nexus、OpenZeppelin、Railsware、Rainforest、RateHawk、Revaly、Safetywing、Scopic、ShaeWellness、Singular、Softr、Stacker、Sumsub、Superside、Verve、Yodo1 Games、企业家组织。

所有来源记录的副本更新时间均早于首次异常批次。执行时仍需使用企业 ID 和生产 `updated_at` 做乐观锁；如果记录在预览后被人工修改，则整家企业跳过并重新审核。

## 无副本的 15 家

Cloudera、Goodnotes、Holafly、Intel 英特尔、Kaplan、Keyrus、Mx51、Sharp Brains、Shell Global 壳牌、Solera Holdings, LLC.、Stimulate、Ultimate Medical Academy、goFLUENT、莉莉丝游戏、赛默飞世尔科技。

这些企业不得根据名称或公开网站自动覆盖。应在后台逐项人工复核，或者从管理员自有历史表格、浏览器导出、截图中恢复。

## 建议执行顺序

1. 导出 26 家生产当前值作为独立回滚文件，文件不进入 Git。
2. 在一个数据库事务内，仅回填上表允许的空字段，并校验 26 个企业 ID 和预期 `updated_at` 全部匹配。
3. 事务后重新统计行业、员工数、成立年份、评分和招聘邮箱，并抽查后台展示。
4. 将 15 家无副本企业加入人工复核清单，不做推断式回填。
5. 地址缺失和 URL 校验准确率作为独立问题处理，避免与本次已证实的数据恢复混合。

