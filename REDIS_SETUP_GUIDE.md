# Redis 双实例配置指南

## 🎯 目标

在一个 Redis 账户下创建两个独立的数据库实例，分别用于开发环境和生产环境。

---

## ✅ 方案一：Upstash（强烈推荐）

Upstash 是专为 Serverless 设计的 Redis 服务，与 Vercel 完美集成。

### 第一步：注册 Upstash 账户

1. 访问 https://console.upstash.com/
2. 使用 GitHub/Google 账号注册（免费套餐足够使用）
3. 完成邮箱验证

### 第二步：创建开发环境数据库

1. 点击右上角 **"Create Database"** 按钮

2. 填写配置：
   ```
   Name: haigoo-development
   Type: Regional (选择离您最近的区域，如 us-west-1)
   Region: 选择 AWS/GCP，推荐选择离用户近的区域
   Eviction: ✅ No Eviction (不淘汰数据)
   TLS: ✅ Enabled (启用 TLS 加密)
   ```

3. 点击 **"Create"** 按钮

4. 数据库创建完成后，点击进入数据库详情页

5. 复制以下信息（稍后配置到 Vercel）：
   ```
   📋 复制这些信息：
   
   Endpoint: xxxxx-xxxxx-xxxxx.upstash.io
   Port: 6379
   Password: xxxxxxxxxxxxxxxxxx
   
   ⚠️ 重要：完整的连接 URL 格式：
   redis://default:你的密码@xxxxx.upstash.io:6379
   ```

### 第三步：创建生产环境数据库

1. 返回 Upstash 控制台首页

2. 再次点击 **"Create Database"**

3. 填写配置：
   ```
   Name: haigoo-production
   Type: Regional (与开发环境选择相同区域，或选择更稳定的区域)
   Region: 推荐选择主要用户所在地区
   Eviction: ✅ No Eviction
   TLS: ✅ Enabled
   ```

4. 点击 **"Create"**

5. 进入数据库详情页，复制连接信息

### 第四步：验证两个数据库已创建

在 Upstash 控制台首页，你应该看到两个数据库：

```
┌─────────────────────────────────────────┐
│  📦 haigoo-development                  │
│  Endpoint: dev-xxxxx.upstash.io         │
│  Region: us-west-1                      │
│  Status: ✅ Active                      │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  📦 haigoo-production                   │
│  Endpoint: prod-xxxxx.upstash.io        │
│  Region: us-west-1                      │
│  Status: ✅ Active                      │
└─────────────────────────────────────────┘
```

---

## 🔧 配置到 Vercel

### 第一步：登录 Vercel Dashboard

访问 https://vercel.com/dashboard

### 第二步：进入项目环境变量设置

1. 选择你的项目（haigoo-remote）
2. 点击 **Settings** → **Environment Variables**

### 第三步：配置开发环境 Redis

点击 **"Add New"** 按钮：

```
┌─────────────────────────────────────────────────────────┐
│ Key (required)                                          │
│ REDIS_URL                                              │
├─────────────────────────────────────────────────────────┤
│ Value (required)                                        │
│ redis://default:你的开发密码@dev-xxxxx.upstash.io:6379  │
├─────────────────────────────────────────────────────────┤
│ Environment (required)                                  │
│ ☐ Production                                           │
│ ☑ Preview          ← 只勾选这个！                       │
│ ☐ Development                                          │
└─────────────────────────────────────────────────────────┘
```

点击 **"Save"**

### 第四步：配置生产环境 Redis

再次点击 **"Add New"** 按钮：

```
┌─────────────────────────────────────────────────────────┐
│ Key (required)                                          │
│ REDIS_URL                                              │
├─────────────────────────────────────────────────────────┤
│ Value (required)                                        │
│ redis://default:你的生产密码@prod-xxxxx.upstash.io:6379 │
├─────────────────────────────────────────────────────────┤
│ Environment (required)                                  │
│ ☑ Production       ← 只勾选这个！                       │
│ ☐ Preview                                              │
│ ☐ Development                                          │
└─────────────────────────────────────────────────────────┘
```

点击 **"Save"**

### 第五步：验证配置

配置完成后，你应该在环境变量列表中看到：

```
REDIS_URL
├─ Production: redis://default:***@prod-xxxxx.upstash.io:6379
└─ Preview:    redis://default:***@dev-xxxxx.upstash.io:6379
```

---

## 🧪 测试连接

### 本地测试

1. 创建 `.env.local` 文件：

```bash
# 使用开发环境 Redis
REDIS_URL=redis://default:你的开发密码@dev-xxxxx.upstash.io:6379
```

2. 启动本地服务器：

```bash
npm run dev
```

3. 访问健康检查接口：

```bash
curl http://localhost:3000/api/health
```

应该返回：

```json
{
  "status": "healthy",
  "environment": "Local",
  "storage": {
    "redis": {
      "configured": true,
      "status": "connected"
    }
  }
}
```

### 测试开发环境

1. 推送代码到 develop 分支：

```bash
git checkout develop
git push origin develop
```

2. 等待部署完成，访问：

```bash
curl https://haigoo-dev.vercel.app/api/health
```

应该返回环境为 "Development"，并且 Redis 状态为 "connected"

### 测试生产环境

1. 推送代码到 main 分支：

```bash
git checkout main
git merge develop
git push origin main
```

2. 访问：

```bash
curl https://haigoo.vercel.app/api/health
```

应该返回环境为 "Production"

---

## 🔐 安全最佳实践

### ✅ 应该做的

1. **使用不同的密码**
   - 开发环境和生产环境必须使用不同的密码
   - Upstash 默认会为每个数据库生成不同的密码

2. **启用 TLS**
   - 确保连接使用 SSL/TLS 加密
   - Upstash 默认启用

3. **定期轮换密码**
   - 每 3-6 个月更换一次密码
   - 在 Upstash 控制台可以重置密码

4. **限制访问**
   - 在 Upstash 中可以设置 IP 白名单（可选）
   - 使用强密码

### ❌ 不应该做的

1. **不要在代码中硬编码连接字符串**
   ```javascript
   // ❌ 错误
   const redis = createClient({ 
     url: 'redis://default:mypassword@prod.upstash.io:6379' 
   })
   
   // ✅ 正确
   const redis = createClient({ 
     url: process.env.REDIS_URL 
   })
   ```

2. **不要将 .env 文件提交到 Git**
   ```bash
   # 确保 .gitignore 包含
   .env
   .env.local
   .env.*.local
   ```

3. **不要在开发环境使用生产凭证**

4. **不要在公共仓库中暴露连接字符串**

---

## 💰 费用说明

### Upstash 免费套餐

✅ **完全免费，足够使用：**
- 10,000 命令/天
- 256 MB 存储
- 无限数据库数量
- 全球所有区域
- TLS 加密
- 99.99% SLA

### 升级选项（如果需要）

如果免费套餐不够用，可以升级到：
- **Pay as you go**: $0.2 / 100K 命令
- **Pro**: $10/月起

---

## 🔄 方案二：使用同一个 Redis 实例 + 不同 Database Number

如果你只想创建一个 Redis 实例，也可以使用不同的数据库编号隔离数据。

### 配置方式

```bash
# 开发环境
REDIS_URL=redis://default:password@your-redis.upstash.io:6379/0

# 生产环境
REDIS_URL=redis://default:password@your-redis.upstash.io:6379/1
```

**注意**：URL 末尾的 `/0` 和 `/1` 表示不同的数据库编号（0-15）

### ⚠️ 不推荐的原因

1. **安全性较低**: 使用相同的密码
2. **隔离性较差**: 数据在同一实例中
3. **难以监控**: 无法分别查看各环境的使用情况
4. **难以扩展**: 未来无法独立升级某个环境

**结论**: 强烈建议使用方案一，创建两个独立的数据库实例。

---

## 🔄 方案三：Vercel KV（替代方案）

如果你不想使用 Upstash，Vercel 自带的 KV 存储也是一个选择。

### 步骤

1. 在 Vercel Dashboard → Storage → Create Database
2. 选择 **KV**
3. 创建两个 KV 存储：
   - `haigoo-kv-production`
   - `haigoo-kv-development`

### 配置

Vercel 会自动为每个环境设置对应的 KV 环境变量：
- `KV_REST_API_URL`
- `KV_REST_API_TOKEN`

**优点**：
- 与 Vercel 原生集成
- 自动配置环境变量
- 无需额外账户

**缺点**：
- 价格较贵（$1 per 100K requests）
- 功能相对简单
- 只能在 Vercel 使用

---

## ✅ 推荐配置总结

### 最佳实践（推荐）

```
开发环境:
  Redis: haigoo-development (Upstash)
  URL: redis://default:dev_password@dev-xxxxx.upstash.io:6379
  Vercel Environment: Preview

生产环境:
  Redis: haigoo-production (Upstash)
  URL: redis://default:prod_password@prod-xxxxx.upstash.io:6379
  Vercel Environment: Production
```

### 配置清单

- [ ] 在 Upstash 创建 `haigoo-development` 数据库
- [ ] 在 Upstash 创建 `haigoo-production` 数据库
- [ ] 复制开发环境的 REDIS_URL
- [ ] 复制生产环境的 REDIS_URL
- [ ] 在 Vercel 配置开发环境变量（Preview）
- [ ] 在 Vercel 配置生产环境变量（Production）
- [ ] 本地测试连接
- [ ] 开发环境测试连接
- [ ] 生产环境测试连接
- [ ] 验证数据隔离

---

## 🆘 常见问题

### Q1: Upstash 免费套餐够用吗？

**A**: 对于大多数项目，完全够用。10,000 命令/天 可以支持：
- 约 300-500 个活跃用户/天
- 数千次页面访问
- 如果不够用，升级也很便宜

### Q2: 如何查看 Redis 使用情况？

**A**: 
1. 登录 Upstash 控制台
2. 点击对应的数据库
3. 查看 Dashboard 中的统计图表

### Q3: 如何迁移数据？

**A**: 如果需要从一个 Redis 迁移到另一个：
```bash
# 使用 redis-cli
redis-cli --rdb dump.rdb
# 导入到新实例
redis-cli --rdb dump.rdb --pipe
```

或者使用 Upstash 提供的迁移工具。

### Q4: 连接失败怎么办？

**A**: 检查清单：
1. URL 格式是否正确（包含 `redis://`）
2. 密码是否正确（不要有多余空格）
3. 端口是否是 6379
4. Vercel 环境变量是否保存
5. 是否需要重新部署

### Q5: 如何备份数据？

**A**: Upstash 自动备份，也可以：
1. 使用 Upstash 的备份功能
2. 定期导出数据
3. 使用 Redis DUMP 命令

---

## 📚 相关链接

- [Upstash 官网](https://upstash.com/)
- [Upstash 文档](https://docs.upstash.com/)
- [Vercel 环境变量文档](https://vercel.com/docs/concepts/projects/environment-variables)
- [Redis 官方文档](https://redis.io/docs/)

---

**下一步**: 按照本指南完成 Redis 配置后，继续 [SETUP_GUIDE.md](./SETUP_GUIDE.md) 的其他步骤。

祝配置顺利！🚀

