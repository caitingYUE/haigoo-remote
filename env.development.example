# ================================
# 开发环境配置示例
# 复制此文件为 .env.local 用于本地开发
# ================================

# 环境标识
NODE_ENV=development

# 站点配置
SITE_URL=https://haigoo-dev.vercel.app

# Redis 配置（开发环境）
# 从 Upstash 获取开发环境的 Redis URL
REDIS_URL=redis://default:your-dev-redis-password@dev-redis.upstash.io:6379

# Vercel KV 配置（开发环境）
# 从 Vercel Dashboard 获取开发环境的 KV 配置
KV_REST_API_URL=https://your-dev-kv-instance.upstash.io
KV_REST_API_TOKEN=your-dev-kv-token

# JWT 配置（开发环境）
JWT_SECRET=dev_jwt_secret_key_change_this_for_production
JWT_EXPIRES_IN=30d

# Google OAuth（开发环境）
# 在 Google Cloud Console 创建开发环境的 OAuth 客户端
GOOGLE_CLIENT_ID=your-dev-google-client-id.apps.googleusercontent.com

# SMTP 配置（开发环境 - 推荐使用 Mailtrap 等测试服务）
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your-mailtrap-username
SMTP_PASS=your-mailtrap-password
FROM_EMAIL=dev@haigoo.com

# ================================
# 注意事项
# ================================
# 1. 此文件仅用于开发/测试环境
# 2. 不要在此文件中使用生产环境的凭证
# 3. 确保 .env.local 已添加到 .gitignore
# 4. 开发环境使用独立的数据库实例

